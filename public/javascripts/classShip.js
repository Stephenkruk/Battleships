class Ship {
    /*  length = lenghth of the ship
        start  = starting coordinate
        end    = ending coordinate
        sunk   = ship "sunk" or not (boolean)
    */

    constructor(length, start, end, sunk){

    }
}